{
  "name": "patch-landing-page",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "NODE_ENV=development node --env-file-if-exists=.env server.js",
    "dev:frontend": "vite",
    "build": "vite build && node scripts/create-route-meta-pages.mjs",
    "preview": "vite preview",
    "start": "NODE_ENV=production node --env-file-if-exists=.env server.js",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "express": "^5.2.1",
    "multer": "^2.3.0",
    "music-metadata": "^11.13.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "resend": "^6.25.0",
    "vite": "^7.0.0"
  },
  "overrides": {
    "nanoid": "3.3.18",
    "postcss": "8.5.26"
  }
}
